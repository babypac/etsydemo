<div class="jumbotron">
  <h1>배우는 곳 너츠볼츠</h1>
  <h2>강좌를 여기서 구매해보세요</h2>
</div>

<div class="center">
  <div class="row">
    <% @listings.each do |listing| %>
        <div class="col-md-3">
          <div class="thumbnail">
            <%= link_to  image_tag(listing.image.url), listing %>
            <div class="caption">
              <h3><%= listing.name %></h3>
              <p><%= number_to_currency(listing.price) %></p>
              <h5><%= "#{listing.user.name}님이 판매 중" %></h5>

            </div>
          </div>
        </div>
    <% end %>
  </div>
</div>

<% if (user_signed_in?) %>
  <%= link_to 'New Listing', new_listing_path, class: "btn btn-link" %>
<% end %>